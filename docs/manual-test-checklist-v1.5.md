# ClipFlow v1.5 全量人工测试清单

> 覆盖：Phase 1（HTTPS/直连改造）· Phase 2（设备管理）· Phase 3（通知栏打开并同步）· Phase 4（登录加固）· Phase 5（工程体验）· 本次基础设施改动（直连 IP + 自签证书指纹固定）
> 版本基线：main @ `92978cd`

## 环境准备

- 服务器：`https://121.196.222.122/api`（直连 IP，自签证书）
  - SSH：`ssh -i /Users/hanyi/Downloads/key241294.pem root@121.196.222.122`
- 三端版本（均为指纹固定版）：
  - Android：已安装（解锁页）
  - macOS：`/Applications/ClipFlow.app`（已运行）
  - Windows：`E:\VSCode\clipflow\build\windows\x64\runner\Release\clipflow.exe`
- 准备：一个测试主密码（+ 一个弱密码如 `123` 用于提示测试）；准备一张图片、一个 PDF/任意文件、一段长文本（>5 万字）

---

## 一、连通性与登录（本次改动核心 · 必测）

| # | 步骤 | 预期 |
|---|------|------|
| 1.1 | 三端分别输入主密码登录 | 均成功进入主页，无 SSL/网络报错 |
| 1.2 | 登录后在解锁页输入 `123` 等弱密码 | **桌面端也出现**「弱密码」非阻断警告条（之前 Mac/Windows 缺失） |
| 1.3 | 手机切换 WiFi ↔ 移动数据后各登录一次 | 均正常（直连 IP 无域名 SNI 过滤） |
| 1.4 | 三端登录后查看状态指示点 | 绿色「已连接」 |
| 1.5 | 服务器 `curl https://121.196.222.122/api/ping` | 返回 `{"code":"SUCCESS"}` |
| 1.6 | 换 IP/网络环境（如热点）登录 | 仍正常（证书固定不依赖网络） |

## 二、跨设备同步（回归 · 必测）

| # | 步骤 | 预期 |
|---|------|------|
| 2.1 | A 端复制一段文本 → B 端约 1-2 秒内出现 | 同步成功，来源设备名正确 |
| 2.2 | A 端复制图片 → B 端收到图片（可预览/全屏） | 图片同步正常 |
| 2.3 | A 端复制文件（PDF）→ B 端收到文件条目 | 文件同步正常（可打开/复制） |
| 2.4 | 复制一条 >5 万字文本 → 其他端收到完整内容 | 不截断 |
| 2.5 | 多选 2-3 条 → 选分隔符（换行/逗号）→ 复制拼接 | 拼接内容正确写入剪贴板 |
| 2.6 | 置顶一条 → 刷新 → 仍在顶部 | 置顶生效 |
| 2.7 | 删除一条（进垃圾箱）→ 其他端同步删除 | 删除同步 |
| 2.8 | 垃圾箱恢复一条 → 其他端恢复 | 恢复同步 |
| 2.9 | 关掉某端再开 → 历史记录仍在 | 本地历史持久化 |

## 三、Phase 2 设备管理

| # | 步骤 | 预期 |
|---|------|------|
| 3.1 | 设置 → 设备管理 | 列表显示各设备，名称为「平台 · 机型」（如 Android · Xiaomi 24129PN74C） |
| 3.2 | 两台同型号设备同时在线 | 默认名不重复（自动加 (2)） |
| 3.3 | 重命名某设备 → 保存 | 列表立即更新；历史来源显示新名 |
| 3.4 | 移除设备 B（确认） | B 的 token 立即失效：B 再同步报 401 并需重新登录；**其他设备不受影响** |
| 3.5 | 移除当前设备 | 本机退出登录；重新登录后设备重新注册 |

## 四、Phase 3 通知栏「打开并同步」（Android）

| # | 步骤 | 预期 |
|---|------|------|
| 4.1 | 设置开启「通知栏同步」 | 通知栏出现常驻通知「ClipFlow 同步 / 就绪」 |
| 4.2 | 另一设备复制内容 → 展开通知点「立即同步」 | App 到前台 + 收到内容 |
| 4.3 | 直接点通知本体 | 同样打开 App + 同步 |
| 4.4 | 连点「立即同步」5 次 | 不卡死、不崩溃、历史无重复条目 |
| 4.5 | 关闭「后台自动同步」、保留「通知栏同步」→ 另一设备复制 → 点「立即同步」 | 仍能收到（一次性下载路径） |
| 4.6 | 锁屏后下拉通知栏点「立即同步」 | App 解锁/到前台（Android 10+ 限制下仅能打开） |

## 五、Phase 4 登录加固

### 5.1 服务端限流（curl，可服务器本机执行）
| # | 命令 | 预期 |
|---|------|------|
| 5.1.1 | 同一 userId 连续 `POST /api/auth` 6 次 | 前 5 次 200、第 6 次 `429 RATE_LIMITED` + `Retry-After` |
| 5.1.2 | 等窗口（默认 60s）后重试同 userId | 恢复 200 |
| 5.1.3 | 换 `X-Forwarded-For: 1.2.3.4` 再连发 | 独立计数（按 IP 隔离） |

### 5.2 客户端
| # | 步骤 | 预期 |
|---|------|------|
| 5.2.1 | 解锁页连输 5 次错误密码 | 第 6 次被本地锁定（倒计时约 60s） |
| 5.2.2 | 输入 `123` / `123456` / `password` | 出现非阻断弱密码警告，仍可继续 |
| 5.2.3 | 备份导出：设置 → 账户与数据 → 导出备份 | 生成 `.clipflow-backup.json`；用文本编辑器打开**只有密文+salt+元数据，无明文** |
| 5.2.4 | 迁移闭环：旧密码解锁 → 导出 → 重启 → 解锁页「切换到其他账户」→ 输入新密码 | 不再报「密码错误」，进入新账户 |
| 5.2.5 | 新账户 → 导入备份 → 输入旧密码 | 文本/图片/文件三类全部恢复且可解密 |
| 5.2.6 | 导入时输错旧密码 | 明确提示「旧密码错误」并中止 |
| 5.2.7 | 造 >5 万字文本 → 导出 → 导入 | 解密为**全量**（不截断） |
| 5.2.8 | 切换账户后检查 | 设备名/deviceId 保留；新账户不继承旧账户历史/盐（数据隔离） |

## 六、Phase 5 工程体验

| # | 步骤 | 预期 |
|---|------|------|
| 6.1 | 触发一次崩溃（如开发版故意抛异常，或真机偶发） | App 不卡死；服务器 `crash_reports` 表有记录（见下命令） |
| 6.2 | 查崩溃记录 | 含异常栈 / 版本 `1.5.0+1` / 时间 / 机型；**无剪贴板明文/密码** |
| 6.3 | 设置页底部版本号 | 显示真实版本（非硬编码 1.4.0） |
| 6.4 | 设置页电池优化行 | 显示「已关闭/未关闭」措辞 |
| 6.5 | 抽查各页面文案（主页/设置/垃圾箱/解锁/备份） | 无缺字、错字、乱码（AppStrings 迁移回归） |

### 服务器查崩溃记录
```bash
ssh -i /Users/hanyi/Downloads/key241294.pem root@121.196.222.122
cd /opt/clipflow && node -e "const db=require('better-sqlite3')('./clipflow.db'); console.log(db.prepare('SELECT id,user_id,app_version,substr(stack,1,80) s,reported_at FROM crash_reports ORDER BY reported_at DESC LIMIT 5').all())"
```

## 七、服务器专项

| # | 检查 | 命令/预期 |
|---|------|-----------|
| 7.1 | 健康检查 | `curl https://121.196.222.122/api/ping` → SUCCESS |
| 7.2 | 限流 | 见 5.1 |
| 7.3 | 崩溃上报 | `curl -X POST https://121.196.222.122/api/crash -H 'Content-Type: application/json' -d '{"stack":"test"}'` → `{"code":"SUCCESS"}`，入库可查 |
| 7.4 | 服务自启 | 重启服务器后 `systemctl status clipflow cloudflared nginx` 均 active |
| 7.5 | 数据库 | 无外键、表齐全（users/devices/clipboard/history/salt/tokens/crash_reports） |

## 八、安全回归

| # | 检查 | 预期 |
|---|------|------|
| 8.1 | 服务器 DB 中剪切板内容 | 全部为密文（`EncryptedData` base64），无明文 |
| 8.2 | token 失效自动重登 | 服务器重启/清 token 后 App 自动重登，无需手动 |
| 8.3 | 证书固定 | 若有人替换服务器证书，App 立即报 SSL 错误（防中间人） |
| 8.4 | 弱密码/限流不误伤 | 正常使用（含 401 自动重登频率）不触发限流 |

## 附：已知说明
- 自动化测试：`flutter analyze` 0 error；`flutter test` 287/287（`clipboard_provider_file_test.dart` 的 10s waitFor 偶发超时属既有高负载抖动，单跑即绿）
- 崩溃上报端到端需真机触发一次崩溃后到服务器查库（6.1/6.2）
- 域名 `api.yihanlife.ccwu.cc`（Cloudflare/隧道）已弃用；App 直连 `121.196.222.122`（自签证书 + 指纹固定）
