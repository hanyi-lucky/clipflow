name: clipflow
description: Cross-platform clipboard sync with E2E encryption
publish_to: 'none'
version: 1.4.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  pointycastle: ^3.9.1
  crypto: ^3.0.6
  shared_preferences: ^2.3.4
  provider: ^6.1.2
  uuid: ^4.5.1
  permission_handler: ^11.3.1
  image: ^4.3.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
